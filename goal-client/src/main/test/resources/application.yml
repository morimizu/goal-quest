app:
  server:
    host: localhost
    port: 8080
  client:
    urls:
      base: http://${app.server.host}:${app.server.port}/goal
      create: ${app.client.urls.base}
      get: ${app.client.urls.base}/{id}


